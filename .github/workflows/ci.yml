name: CI/CD Security Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  security-pipeline:
    name: Build, Test, SAST, SCA, Container, and IaC Scan
    runs-on: ubuntu-latest

    env:
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Run Unit Tests
        run: npm test

      - name: Fail on critical vulnerabilities
        run: npm audit --audit-level=high

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ecommerce-app:ci

      - name: Scan dependencies and local filesystem with Trivy
        uses: aquasecurity/trivy-action@0.24.0
        with:
          scan-type: fs
          scan-ref: .
          vuln-type: library,os
          severity: CRITICAL,HIGH
          exit-code: 1
          format: table
          ignore-unfixed: true
          skip-dirs: .venv

      - name: Scan container image with Trivy
        uses: aquasecurity/trivy-action@0.24.0
        with:
          scan-type: image
          image-ref: ecommerce-app:ci
          vuln-type: library,os
          severity: CRITICAL,HIGH
          exit-code: 1
          format: table
          ignore-unfixed: true

      - name: Run Checkov IaC scan
        uses: bridgecrew/checkov-action@v12.2.0
        with:
          directory: infra/terraform
          framework: terraform
          output_format: cli
          quiet: false
          soft_fail: false

      - name: SonarCloud / SonarQube Scan
        if: ${{ secrets.SONAR_TOKEN != '' }}
        uses: sonarsource/sonarcloud-github-action@v1
        with:
          args: >
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}
            -Dsonar.host.url=${{ env.SONAR_HOST_URL }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.qualitygate.wait=true

      - name: Upload Test Coverage Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-coverage-report
          path: coverage/

      - name: Upload Pipeline Summary
        if: always()
        run: |
          mkdir -p docs
          echo "Enterprise DevSecOps Pipeline Report" > docs/pipeline-report.txt
          echo "Timestamp: $(date -u)" >> docs/pipeline-report.txt
          echo "Commit SHA: ${{ github.sha }}" >> docs/pipeline-report.txt
          echo "Sonar Scan Performed: ${{ secrets.SONAR_TOKEN != '' }}" >> docs/pipeline-report.txt
          echo "Trivy Scan Performed: true" >> docs/pipeline-report.txt
          echo "Checkov IaC Scan Performed: true" >> docs/pipeline-report.txt
          echo "==========================================" >> docs/pipeline-report.txt
          cat docs/pipeline-report.txt

      - name: Upload Pipeline Summary Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pipeline-report
          path: docs/pipeline-report.txt

