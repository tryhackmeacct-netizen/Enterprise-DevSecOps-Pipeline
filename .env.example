# Application Configuration
PORT=3000
NODE_ENV=development

# Session validation constraints (example configurations)
# SESSION_SECRET=example-secret-placeholder-do-not-use-in-production

# External service secrets (do NOT set real secrets here - use repository secrets or a secrets manager)
# PAYMENT_API_SECRET_KEY=
